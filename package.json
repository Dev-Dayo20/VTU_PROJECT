{
  "name": "devd-plug",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "admin": "cd admin && npm run start",
    "client": "cd user && npm run start",
    "server": "nodemon server/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run admin\"",
    "build": "cd user && npm run build",
    "lint": "cd user && npm run lint",
    "preview": "cd user && npm run preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "nodemon": "^3.1.7"
  }
}
